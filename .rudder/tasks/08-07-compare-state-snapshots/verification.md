# 验收记录：按章节状态检查点

## 已完成

- 新增版本化 `project_state_checkpoints`，绑定项目、章节、分析任务、正文哈希、配置版本和状态 Schema。
- 状态快照覆盖角色、关系、组织、组织成员、职业、角色职业、伏笔和结构化记忆，并保留源实体 ID 供克隆时统一映射。
- 标准分析在关系状态、伏笔、记忆和向量写入全部成功后、任务标记完成前创建检查点；失败分析不创建可用节点。
- 只有首章或拥有正文版本一致的上一章有效节点、且项目不存在已物化后续分析时，新节点才标记为 `valid`。
- 正文修改和流水线回滚会立即失效当前章及之后节点；读取接口还会再次核对正文哈希。
- 改写已分析章节时，从 X-1 有效节点恢复关系型状态，清理 X 及之后向量记忆、PlotAnalysis，并把后续分析标记为 `superseded` 后顺序重算。
- 缺少可靠前置检查点时拒绝覆盖，不从残缺旧数据猜测历史。
- 旧书可显式登记“当前最新可靠节点”；新任务按正文哈希证明，旧任务按 PlotAnalysis 时间不早于正文更新时间证明。
- 新增可继承节点只读接口和旧书最新节点登记接口，均复用项目权限校验。

## 自动验收

- 状态检查点测试：14/14 通过。
- 章节生命周期回归：12/12 通过。
- Pipeline 回归：7/7 通过。
- 项目创作配置回归：5/5 通过。
- OpenAI 客户端兼容回归：9/9 通过。
- 真实 SQLite 等价性测试：捕获后修改角色心理、职业阶段、关系亲密度、伏笔并新增未来角色；恢复结果与原快照逐字段一致。
- 大快照测试：1000 个角色的版本化 JSON 可稳定往返，体积低于 1 MB。
- 中断测试：向量清理失败时不会开始关系型状态恢复。
- PostgreSQL 迁移：`e4b1d2a3 (head)`。
- SQLite 本次迁移从 `d0a9f37a` 升级到 `e5c2d3b4` 并回退通过。
- 后端 `compileall`：通过。
- 真实服务：`/health` 正常；OpenAPI 248 条，新增状态检查点路由 2 条。
- `git diff --check`：通过。

## 边界说明

- 第 1 章没有“写作前状态检查点”的旧书仍不能安全覆盖；不能证明的节点不会展示。
- 本任务提供节点和恢复能力，不创建副本。项目深复制与新 ID 映射由 `08-07-compare-project-clone` 负责。
- 伏笔和结构化记忆进入快照，是因为这些行会被后续章节更新；只按章节号复制当前表会泄漏未来状态。
