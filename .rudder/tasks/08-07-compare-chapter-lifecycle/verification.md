# 验收记录：正式章节与分析生命周期

## 已完成

- 分析任务绑定正式正文 SHA-256，旧正文的 completed 任务不能放行下一章。
- 普通分析与分析候选共用角色、职业、关系、组织、伏笔和提示词上下文。
- PlotAnalysis、关系库/向量记忆、职业、角色关系、组织、伏笔统一物化，全部成功后才 completed。
- 流式单章、后台单章、顺序批量共用正式正文落库事务。
- 顺序批量逐章强制等待正式分析；分析失败即中止。
- 已分析正文的手工覆盖和局部重写暂时返回 409，等待状态检查点支持安全重建。
- 章节/分析候选可继续生成、预览、编辑和重试；采用入口暂时返回 409。

## 自动验收

- 生命周期单元测试：12/12 通过。
- 原有 OpenAI 客户端回归：9/9 通过。
- 容器 `compileall`：通过。
- PostgreSQL 迁移：当前为 `a8d6c047 (head)`。
- SQLite 本次迁移升级和回退：通过；仓库旧的 `b3e9f105` 迁移仍阻止从零全量升级，已作为既有问题记录。
- 真实服务：`/health` 正常，OpenAPI 244 条路径、章节相关 33 条，重构前后数量一致。
- `git diff --check`：通过。

## 后续边界

Pipeline 对章节模型、分析模型、Skill、风格和 MCP 的完整传递由依赖任务
`08-07-compare-pipeline-foundation` 实施；本任务提供其必须复用的正文落库和分析物化入口。
历史正文重建与任意继承节点由 `08-07-compare-state-snapshots` 实施。
