# 验收记录：Pipeline 接回原有创作底座

## 已完成

- 流水线启动、暂停后应用配置、继续运行时，均从本书的项目创作配置冻结运行快照。
- 章节写作和章节分析分别使用配置中的服务与模型，不再共用章节模型。
- Skill、写作风格、叙事视角、MCP 开关和指定插件列表已接入原章节生成链路。
- 正文先经过正式章节落库，再同步等待正式分析完成；记忆、伏笔、角色、关系、组织或向量写入失败时不会推进下一章。
- 上一章正文存在但分析未完成时，流水线进入可恢复暂停，不会误判为当前卷完成。
- 生成内容不足在正式落库前失败，避免写入短正文后再直接清空数据库。
- 运行中禁止修改配置；暂停或待审阅时可显式应用新配置版本。
- 流水线配置面板改为直接读写项目创作配置，补齐 Skill、风格、MCP、叙事视角、正文参数、预算和检查点控件。
- 项目尚未启动流水线时也可先保存创作配置；失效资源会在面板显示明确错误。

## 自动验收

- Pipeline 配置与生命周期测试：7/7 通过。
- 章节生命周期回归：12/12 通过。
- 项目创作配置回归：5/5 通过。
- OpenAI 客户端兼容回归：9/9 通过。
- 前端 TypeScript：通过。
- 本次前端文件定向 ESLint：通过。
- 前端生产构建：通过，后端可正常返回首页与构建后的 JS/CSS 资源。
- 后端 `compileall`：通过。
- 真实服务：`/health` 正常；OpenAPI 246 条，其中项目创作配置 2 条、Pipeline 10 条。
- `git diff --check`：通过。

## 已知基线问题

- 全量前端 ESLint 仍有 8 个错误、2 个警告，均位于本任务未修改的 `Chapters.tsx`、`ProjectWizardNew.tsx`、`ThemeTemplates.tsx`、`AIServiceSelector.tsx` 和 `AIUsageLogs.tsx`。
- 自动验收使用模拟 AI 返回验证参数和状态流转，没有调用付费 LLM。人工冒烟时应在测试书中选择低成本模型，生成一章并确认正文分析、记忆、伏笔和角色状态均出现后再停止流水线。

## 后续边界

本任务只保证一本书内部的 Pipeline 复用原有底座。章节继承时的历史状态冻结由 `08-07-compare-state-snapshots` 负责；候选采用由 `08-07-compare-candidate-reintegration` 负责；完整项目副本创建由 `08-07-compare-project-clone` 负责。
