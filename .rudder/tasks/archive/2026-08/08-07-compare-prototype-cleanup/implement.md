# 实施计划：原型清理

1. 提交一：移除前端入口、弹窗、路由和并排阅读页；运行 TypeScript 构建。
2. 提交二：移除后端 clone-for-compare 路由、Schema、服务和原型测试；运行后端编译和导入检查。
3. 提交三：把原三个任务标记为冻结并指向新任务树；运行 `git diff --check`。

回滚：每个提交可独立恢复；不执行数据库迁移或删除现有数据库记录。
