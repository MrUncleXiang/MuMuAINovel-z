# 技术设计：单副本创建 UI

从书架和项目详情提供“创建副本”命令，复用一个模态面板。面板先读取源书可继承节点和配置摘要，再让用户选择“仅复制设定”或一个可靠检查点。

继承选项展示截止章，并用大白话说明会自动包含正文、分析、记忆、伏笔和人物状态；不允许用户取消其中会破坏续写一致性的部分。标题默认使用源书名加模型/用途后缀，但可编辑。

创建期间禁止重复提交，失败保留表单并显示具体原因。成功后刷新书架并进入新项目的配置/详情页，不自动启动 Pipeline。移动端使用单列布局，长书名和长模型名不能溢出。
