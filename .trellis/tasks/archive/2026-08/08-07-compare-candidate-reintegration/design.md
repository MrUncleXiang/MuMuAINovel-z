# 技术设计：候选重新接入

候选生成仍使用现有共享比较模块和冻结输入。目标适配器只负责把选中的输出交给正式模块：章节候选交给正式章节落库模块，分析候选交给标准分析物化模块。

采用事务先锁定批次与正式目标，再校验正文版本和检查点版本。正式状态成功更新后才标记候选已采用。分析物化失败时整个采用保持未完成，可安全重试。

批量候选保留持久化和进度查看，但 UI 文案与接口契约明确“各章独立预览”，不把未采用、未分析的后续章当作连续正文。
